{
    "name": "daurensky/news-test",
    "version": "1.0.0",
    "authors": [
        {
            "name": "daurensky",
            "email": "dkambarov17@gmail.com"
        }
    ],
    "require": {
        "php": "^8.1",
        "smarty/smarty": "^5.7",
        "vlucas/phpdotenv": "^5.6",
        "php-di/php-di": "^7.1"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "setup": [
            "composer install",
            "@php -r \"file_exists('.env') || copy('.env.example', '.env');\"",
            "npm install",
            "npm run build"
        ]
    }
}
